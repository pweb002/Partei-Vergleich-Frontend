Für alle Befehle ist es wichtig in dem Projekt Ordner zu sein, es wird angenommen, dass das hier alles auf Linux ausgeführt wird.

# Abhängigkeiten installieren

```bash
pip3 install -r requirements.txt
```

